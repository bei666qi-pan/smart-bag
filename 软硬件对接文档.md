# Smart Schoolbag V5.0 软硬件对接文档

## 0. 文档定位

本文档按当前仓库真实代码整理，目标是让硬件、前端、部署三方在同一份协议和链路认知上联调。

本文只描述当前代码已经实现的行为，不描述理想方案，也不默认任何“未来会改”的接口。

## 1. 当前系统的 5 条真实链路

### 1.1 浏览器页面访问链路

- 仪表盘：`/`
- 视觉页：`/vision`
- 位置页：`/location`
- 交互页：`/interaction`

### 1.2 浏览器 MQTT 链路

浏览器通过 `NEXT_PUBLIC_MQTT_URL` 建立 WebSocket MQTT 连接，`NEXT_PUBLIC_MQTT_PATH` 只在 URL 本身未带路径时作为补充。

当前示例配置见 [.env.example](C:/Users/21276/OneDrive/Desktop/智能书包/.env.example)：

```bash
NEXT_PUBLIC_MQTT_URL=wss://bag.versecraft.cn/mqtt
NEXT_PUBLIC_MQTT_PATH=/mqtt
```

这条链路只给网页用，硬件不要连接 WSS 地址。

### 1.3 硬件 MQTT 链路

硬件应连接与服务端 `MQTT_SERVER_URL` 指向同一个 Broker 的 TCP MQTT 端口。

示例：

```bash
MQTT_SERVER_URL=mqtt://mqtt.bag.versecraft.cn:1883
```

硬件侧请使用：

- Host：`mqtt.bag.versecraft.cn`
- Port：`1883`
- Protocol：原生 TCP MQTT

不要把 `NEXT_PUBLIC_MQTT_URL` 里的 `wss://.../mqtt` 直接给硬件使用。

### 1.4 服务端 MQTT -> Redis 镜像链路

服务端守护进程由 [instrumentation.ts](C:/Users/21276/OneDrive/Desktop/智能书包/instrumentation.ts) 和 [lib/iot/redis-mqtt-daemon.ts](C:/Users/21276/OneDrive/Desktop/智能书包/lib/iot/redis-mqtt-daemon.ts) 触发：

- 订阅 `v5/bag/#`
- 把最近一次状态写入 Redis 的 `bag:latest`
- 页面刷新时由 `/api/iot/status` 或 `/api/iot/state` 读取初始值

这条链路依赖：

- `MQTT_SERVER_URL`
- `REDIS_URL`

### 1.5 视觉 AI 链路

视觉分析已切到 NewAPI 双模型：

```text
真实图片 -> bag-image -> 结构化 JSON -> bag-text -> analysis / suggestion / screen_text / severity
```

服务端只读取：

- `NEWAPI_BASE_URL`
- `NEWAPI_API_KEY`

模型别名固定为：

- `bag-image`
- `bag-text`

代码里不再通过环境变量切具体底模。

## 2. 当前环境变量分工

### 2.1 前端可见

| 变量 | 用途 | 备注 |
|---|---|---|
| `NEXT_PUBLIC_MQTT_URL` | 浏览器 MQTT WebSocket 地址 | 例如 `wss://bag.versecraft.cn/mqtt` |
| `NEXT_PUBLIC_MQTT_PATH` | 浏览器 MQTT 路径补充 | URL 已带路径时通常可忽略 |
| `NEXT_PUBLIC_AMAP_KEY` | 高德地图 Key | 仅位置页使用 |
| `NEXT_PUBLIC_AMAP_SECURITY_CODE` | 高德安全密钥 | 仅位置页使用 |
| `NEXT_PUBLIC_ESP32_STREAM_URL` | 局域网视频流地址 | 仅视觉页 LAN 模式使用 |

### 2.2 服务端私有

| 变量 | 用途 | 备注 |
|---|---|---|
| `MQTT_SERVER_URL` | 服务端守护进程连接 Broker | 用于 MQTT -> Redis 镜像 |
| `REDIS_URL` | Redis 存储 | 用于 `bag:latest` |
| `NEWAPI_BASE_URL` | NewAPI 基础地址 | 自动兼容是否带 `/v1` |
| `NEWAPI_API_KEY` | NewAPI API Key | 严禁暴露到前端 |

### 2.3 当前代码里的额外固定值

视觉快照上传接口 [app/api/camera/latest/route.ts](C:/Users/21276/OneDrive/Desktop/智能书包/app/api/camera/latest/route.ts) 目前要求请求头：

```http
x-device-token: bag_secret_2026
```

这是代码中的静态值，不是环境变量。联调时必须对齐。

## 3. MQTT Topic 与协议

当前代码真实使用的主题如下：

| Topic | 方向 | 用途 |
|---|---|---|
| `v5/bag/status` | 设备 -> 云端 | 在线/离线状态 |
| `v5/bag/sensors` | 设备 -> 云端 | 电量/温度/湿度 |
| `v5/bag/gps` | 设备 -> 云端 | GPS 定位 |
| `v5/bag/cmd` | 云端 -> 设备 | 下行命令 |
| `v5/bag/cmd/ack` | 设备 -> 云端 | 命令 ACK |

浏览器实际订阅：

- `v5/bag/status`
- `v5/bag/sensors`
- `v5/bag/gps`
- `v5/bag/cmd/ack`

服务端守护进程订阅：

- `v5/bag/#`

## 4. 上行协议：设备 -> 云端

### 4.1 在线状态

Topic：`v5/bag/status`

上线时发送：

```json
{ "status": "online" }
```

建议把 MQTT Last Will 设置成：

```json
{ "status": "offline" }
```

建议：

- QoS：`1`
- Retain：`true`

### 4.2 传感器数据

Topic：`v5/bag/sensors`

推荐 payload：

```json
{
  "battery": 88,
  "temp": 24.5,
  "humid": 45
}
```

字段名请保持：

- `battery`
- `temp`
- `humid`

网页和 Redis 镜像都按这三个字段读取。

### 4.3 GPS 数据

Topic：`v5/bag/gps`

推荐 payload：

```json
{
  "lat": 31.230412,
  "lng": 121.473701
}
```

当前代码同时兼容：

```json
{
  "latitude": 31.230412,
  "longitude": 121.473701
}
```

坐标要求：

- 硬件上传 `WGS-84` 原始坐标
- 不要在硬件端自行转 GCJ-02
- 前端地图层会处理转换显示

### 4.4 命令 ACK

Topic：`v5/bag/cmd/ack`

推荐 payload：

```json
{
  "cmd_id": "与下行命令中的 id 相同",
  "status": 0,
  "msg": "OK"
}
```

说明：

- 下行命令发出时字段名是 `id`
- ACK 返回时字段名必须是 `cmd_id`
- `status=0` 表示成功，非 0 表示失败

## 5. 下行协议：云端 -> 设备

下行 Topic 固定为：

```text
v5/bag/cmd
```

### 5.1 模式切换

```json
{ "id": "uuid", "action": "mode_switch", "value": "focus_mode" }
```

```json
{ "id": "uuid", "action": "mode_switch", "value": "normal_mode" }
```

### 5.2 屏幕文本

```json
{ "id": "uuid", "action": "screen_text", "value": "记得带作业本" }
```

### 5.3 设备必须做的事

- 订阅 `v5/bag/cmd`
- 解析 `action` 与 `value`
- 执行后向 `v5/bag/cmd/ack` 回 ACK

## 6. 页面真实行为说明

### 6.1 顶部状态栏的三个状态不是一回事

当前顶部栏 [components/dashboard/top-bar.tsx](C:/Users/21276/OneDrive/Desktop/智能书包/components/dashboard/top-bar.tsx) 会同时展示：

- `MQTT 在线/连接中/离线/异常`
- `设备 在线/离线`
- `API 正常/拉取中/异常/未拉取`

含义分别是：

- `MQTT`：浏览器是否连上 Broker
- `设备`：是否收到 `v5/bag/status = online`
- `API`：是否能从 `/api/iot/status` 读取 Redis 镜像

### 6.2 页面刷新后的数据来源

刷新后首先读取：

- `/api/iot/status`

该接口本质上转到：

- `/api/iot/state`

返回字段包括：

```json
{
  "status": "online",
  "deviceOnline": true,
  "battery": 88,
  "temp": 24.5,
  "humid": 45,
  "lat": 31.230412,
  "lng": 121.473701,
  "lastSeenAt": "2026-04-10T12:34:56.000Z"
}
```

如果 Redis 或守护进程不可用，会回退到空状态，而不是伪造设备在线。

### 6.3 服务端守护诊断入口

可以直接查看：

- `/api/iot/daemon-status`

该接口会返回：

- 环境变量是否配置
- 守护进程是否启动
- Redis / MQTT 是否连通
- 最近一次镜像时间
- 最近错误

## 7. 视频链路

视觉页 [components/dashboard/vision-section.tsx](C:/Users/21276/OneDrive/Desktop/智能书包/components/dashboard/vision-section.tsx) 现在只在“真实画面已就绪”时允许 AI 分析。

### 7.1 LAN 模式

LAN 模式直接读取：

- `NEXT_PUBLIC_ESP32_STREAM_URL`

典型示例：

```text
http://<ESP32_IP>:81/stream
```

要求：

- 能被浏览器直接访问
- 能持续返回真实 MJPEG 画面
- 如果想在页面里截帧给 AI，还需要浏览器允许画布读取该图像

如果公网页面是 HTTPS，而摄像头流是 HTTP，会被浏览器 Mixed Content 拦截。

### 7.2 WAN 模式

WAN 模式使用快照接口：

- `POST /api/camera/latest`
- `GET /api/camera/latest`

设备上传要求：

- Header：`x-device-token: bag_secret_2026`
- Content-Type：`multipart/form-data`
- 文件字段名：`image`

成功响应：

```json
{
  "success": true,
  "message": "快照上传成功",
  "timestamp": "2026-04-10T12:34:56.000Z"
}
```

无快照时，`GET /api/camera/latest` 会返回：

- HTTP `404`
- `{"success":false,"message":"暂无快照"}`

## 8. 视觉 AI 链路

当前分析按钮调用 [app/actions/analyze-image.ts](C:/Users/21276/OneDrive/Desktop/智能书包/app/actions/analyze-image.ts)。

### 8.1 阶段一：`bag-image`

输入：图片

输出：

```json
{
  "objects": ["书本", "笔袋", "水杯"],
  "scene": "书桌",
  "risks": ["未发现明显危险物"],
  "confidence": 0.92,
  "raw_summary": "画面中主要是学习用品"
}
```

### 8.2 阶段二：`bag-text`

输入：`bag-image` 的结构化结果

输出：

```json
{
  "analysis": "检测到书本、笔袋和水杯，当前物品状态正常。",
  "suggestion": "建议确认是否已带齐作业本。",
  "screen_text": "物品齐全",
  "severity": "low"
}
```

### 8.3 页面展示规则

- 没有真实画面时，不允许分析
- 不再展示伪造 AI 日志
- 分析结果会同时展示结构化识别结果和中文结论

## 9. 交互页消息发送链路

交互页 [components/dashboard/interaction-section.tsx](C:/Users/21276/OneDrive/Desktop/智能书包/components/dashboard/interaction-section.tsx) 现在有两条不同入口：

### 9.1 “发送消息”按钮

流程：

```text
用户输入文本 -> reviewDeviceMessageAction -> bag-text 生成 review ->
如果 severity=high 则阻止自动下发 ->
否则在 MQTT 已连通且设备在线时自动发送 screen_text
```

返回结构包含：

- `analysis`
- `suggestion`
- `screen_text`
- `severity`
- `should_send`
- `decision_reason`

### 9.2 “直接发送 screen_text”按钮

这是 IoT 调试入口，不经过 AI，直接向 `v5/bag/cmd` 发命令，保留给联调用。

## 10. 建议的硬件上报频率

- `v5/bag/status`：连接建立后立即发，异常掉线靠 LWT
- `v5/bag/sensors`：每 `3-5 秒`
- `v5/bag/gps`：移动中 `1 秒/次`，静止时 `5-10 秒`
- `v5/bag/cmd/ack`：收到命令后尽快返回

## 11. 联调最小闭环

### 11.1 验证 Broker

先确认：

- 硬件能连上 TCP MQTT Broker
- 网页能连上 WSS MQTT Broker
- 服务端 `MQTT_SERVER_URL` 能连上同一 Broker

### 11.2 验证状态上报

发送：

```json
{ "status": "online" }
```

预期：

- 顶部“设备”变在线
- `/api/iot/daemon-status` 的 `lastMirrorAt` 更新

### 11.3 验证传感器

发送：

```json
{ "battery": 88, "temp": 24.5, "humid": 45 }
```

预期：

- 顶部温湿度、电量更新
- 刷新页面后仍能读到 Redis 镜像值

### 11.4 验证 GPS

发送：

```json
{ "lat": 31.230412, "lng": 121.473701 }
```

预期：

- 位置页 marker 更新
- `/api/iot/status` 返回 `lat/lng`

### 11.5 验证命令闭环

在 `/interaction`：

- 点击 `发送 mode_switch(focus_mode)`
- 或点击 `发送消息`

设备收到 `v5/bag/cmd` 后执行，再发：

```json
{ "cmd_id": "与命令中的 id 相同", "status": 0, "msg": "OK" }
```

预期：

- 调试面板里 `pendingCmd` 消失
- `lastCmdAck` 更新
- 页面出现成功提示

### 11.6 验证视觉链路

任选其一：

- LAN：浏览器能直接看到 `NEXT_PUBLIC_ESP32_STREAM_URL`
- WAN：设备能上传 `POST /api/camera/latest`

然后在 `/vision` 点 `AI 分析`，确认页面出现真实 NewAPI 结果。

## 12. 常见故障排查

### 12.1 网页 MQTT 在线，但设备一直离线

说明浏览器连上了 Broker，但设备没有成功上报：

- `v5/bag/status = online`

优先检查：

- 设备连的是不是 TCP MQTT 而不是 WSS
- Client ID 是否唯一
- LWT / 在线消息是否发到了正确 topic

### 12.2 实时数据显示正常，刷新后全没了

优先检查：

- `REDIS_URL` 是否配置
- `MQTT_SERVER_URL` 是否配置
- `/api/iot/daemon-status` 是否显示 daemon 已启动

### 12.3 命令发出后一直 pending

优先检查：

- 设备是否订阅 `v5/bag/cmd`
- ACK 是否发回 `v5/bag/cmd/ack`
- ACK 字段是不是 `cmd_id` 而不是 `id`

### 12.4 视觉页无法分析

优先检查：

- 当前是否真的有画面或快照
- WAN 模式下 `/api/camera/latest` 是否返回图片
- 服务端是否配置 `NEWAPI_BASE_URL` 与 `NEWAPI_API_KEY`
- NewAPI 后台是否存在 `bag-image` / `bag-text` 两个别名

### 12.5 “发送消息”没有自动下发设备

这不一定是错误，可能是：

- `bag-text` 返回了 `severity=high`
- 当前 MQTT 未连接
- 当前设备离线

页面会保留评估结果和原因说明。

## 13. 给硬件同学的最终 checklist

- 设备连 TCP MQTT，不连浏览器 WSS 地址
- 严格使用现有 topic：`status/sensors/gps/cmd/cmdAck`
- 上报字段优先用 `battery/temp/humid/lat/lng`
- 下行命令读取 `id/action/value`
- ACK 必须回 `cmd_id/status/msg`
- GPS 发 WGS-84 原始坐标
- WAN 快照上传要带 `x-device-token: bag_secret_2026`
- 页面刷新状态依赖服务端 `MQTT_SERVER_URL + REDIS_URL`
- 视觉 AI 现在固定走 `bag-image + bag-text`
- 交互页“发送消息”会先经过 `bag-text` 决策，不是原样直发

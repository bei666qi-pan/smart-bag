# Smart Schoolbag V5.0 软硬件对接文档

本文档是当前仓库唯一维护中的软硬件对接说明。联调时请以本文件为准，不再参考历史副本或导出的 PDF 版本。

## 0. 先看这 4 条

1. 硬件 MQTT 只走 `mqtt.bag.versecraft.cn:1883`，不要连浏览器 WSS。
2. 浏览器 MQTT 只走 `wss://bag.versecraft.cn/mqtt`，不要再混用其他生产 WSS 地址。
3. `Broker 在线 != 设备在线`，设备在线只以 `v5/bag/status` 为准。
4. 页面刷新后的初始状态依赖 `MQTT_SERVER_URL + REDIS_URL + /api/iot/state`，不是浏览器内存。

## 1. 地址与连接方式

### 浏览器 MQTT

- 地址：`wss://bag.versecraft.cn/mqtt`
- 用途：前端页面实时订阅状态、传感器、GPS、命令 ACK
- 说明：这是浏览器专用地址，不给硬件使用

### 硬件 MQTT

- Host：`mqtt.bag.versecraft.cn`
- Port：`1883`
- 协议：TCP MQTT
- 用途：设备上报状态与接收命令

### 服务端守护进程

- 环境变量：`MQTT_SERVER_URL`
- 推荐值：`mqtt://mqtt.bag.versecraft.cn:1883`
- 作用：服务端订阅 `v5/bag/#` 并镜像到 Redis

## 2. 当前真实链路

```text
设备 -> MQTT Broker -> 浏览器实时 UI
设备 -> MQTT Broker -> 服务端 daemon -> Redis -> /api/iot/state -> 页面刷新初始值
```

关键说明：

- `Broker 在线 != 设备在线`
- `/api/iot/status` 是 `/api/iot/state` 的别名
- 页面刷新后的初始状态依赖 `MQTT_SERVER_URL + REDIS_URL`
- `/api/iot/daemon-status` 用于排查部署链路是否正常

## 3. MQTT Topic 约定

### 设备上报

- `v5/bag/status`
- `v5/bag/sensors`
- `v5/bag/gps`
- `v5/bag/cmd/ack`

### 设备订阅

- `v5/bag/cmd`

## 4. Payload 约定

### 设备在线状态

Topic:

```text
v5/bag/status
```

Payload:

```json
{ "status": "online" }
```

建议同时配置 LWT：

```json
{ "status": "offline" }
```

### 传感器

Topic:

```text
v5/bag/sensors
```

Payload:

```json
{
  "battery": 88,
  "temp": 24.5,
  "humid": 45
}
```

### GPS

Topic:

```text
v5/bag/gps
```

Payload:

```json
{
  "lat": 31.230412,
  "lng": 121.473701
}
```

说明：

- 硬件上传 WGS-84 原始坐标
- 前端地图层负责转成 GCJ-02 显示
- 兼容 `latitude/longitude`，但推荐统一成 `lat/lng`

### 下行命令

Topic:

```text
v5/bag/cmd
```

Payload:

```json
{ "id": "uuid", "action": "screen_text", "value": "记得带作业本" }
```

也支持：

```json
{ "id": "uuid", "action": "mode_switch", "value": "focus_mode" }
```

### ACK

Topic:

```text
v5/bag/cmd/ack
```

Payload:

```json
{ "cmd_id": "uuid", "status": 0, "msg": "OK" }
```

注意：

- 下行命令字段叫 `id`
- ACK 字段必须叫 `cmd_id`

## 5. API 约定

### `/api/iot/state`

读取 Redis 中最新镜像值。

示例：

```json
{
  "status": "online",
  "deviceOnline": true,
  "battery": 88,
  "temp": 24.5,
  "humid": 45,
  "lat": 31.230412,
  "lng": 121.473701,
  "lastSeenAt": "2026-04-11T10:20:30.000Z"
}
```

### `/api/iot/status`

`/api/iot/state` 的别名。

### `/api/iot/daemon-status`

只读诊断接口，用来判断服务端守护进程是否真的工作：

```json
{
  "started": true,
  "redisConfigured": true,
  "mqttServerConfigured": true,
  "redisConnected": true,
  "mqttConnected": true,
  "subscribed": true,
  "lastMirroredAt": "2026-04-11T10:20:30.000Z",
  "lastMirroredTopic": "v5/bag/gps",
  "lastError": null
}
```

这个接口不会泄露 Redis URL、token、key 等敏感值。

## 5.1 排障建议

如果页面实时状态和刷新后的状态不一致，按这个顺序排查：

1. 浏览器是否已连上 `wss://bag.versecraft.cn/mqtt`
2. 设备是否真的上报了 `v5/bag/status`
3. `/api/iot/daemon-status` 是否显示 daemon 已启动并已订阅
4. `/api/iot/status` 是否能读到 Redis 镜像

这四步能把“浏览器订阅问题”和“部署链路问题”很快分开。

## 6. 页面行为说明

### 仪表盘

- 只显示真实实时值或空态
- 没有数据时显示 `—` 或“暂无数据”

### 位置页

- 右下角坐标文本直接绑定 `gpsCoords`
- marker 跟随 `gpsCoords`
- 地图中心跟随 `gpsCoords`

### 交互页

- `AI 评估` 只做文本评估和生成 `screen_text`
- `发送到设备` 才会真实下发 MQTT 命令
- 收到 `cmd/ack` 后会更新 ACK 状态并清空 `pendingCmd`

## 7. 视频链路

### LAN

- 使用 `NEXT_PUBLIC_ESP32_STREAM_URL`
- 只适合同网段联调
- HTTPS 页面下如果视频流还是 HTTP，会被浏览器 Mixed Content 拦截

### WAN

- 上传：`POST /api/camera/latest`
- 拉取：`GET /api/camera/latest`
- 远程环境建议使用这个快照模式

## 8. 联调最小闭环

1. 确认 `/api/iot/daemon-status` 显示 `started=true`
2. 设备发布 `v5/bag/status`
3. 设备发布 `v5/bag/sensors`
4. 设备发布 `v5/bag/gps`
5. 页面在 `/interaction` 下发 `screen_text`
6. 设备返回 `v5/bag/cmd/ack`

达到以上 6 步时，才可以认为“软硬件主链路已打通”。

## 9. 常见误判

- 浏览器 WSS 503 不能通过前端写死 fallback 来“修复”
- Broker 连通不代表设备已经在线
- 页面刷新没数据时，优先看 Redis 和 daemon，而不是先怀疑前端订阅
- `/api/iot/status` 缺数据时，不代表浏览器 MQTT 一定断了；也可能是服务端镜像链路没跑起来

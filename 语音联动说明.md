# 网页端 · 语音联动改动说明（仪表盘显示语音指令）

> 日期：2026-06-09 · 配套：Jetson 语音助手「小乐」←→ 书包 ESP32 的控制联动。
> 完整协议见 `../jetson边缘智能/语音联动说明.md`；固件侧见 `../硬件传感器/语音联动说明.md`。
> 本文件只说明 **网页端这一侧改了什么**。所有改动处均带 `[语音联动]` 注释，可全局搜索定位。

---

## 1. 做了什么

让仪表盘的「语音助手（小乐）」卡片**显示最近一条语音发起的书包控制命令**（如「进入专注模式」「唤醒（正在聆听）」）。
数据来自 Jetson 发布的 `v5/bag/voice/cmd`，由服务端 daemon 镜像进 Redis，并经实时 MQTT + 刷新恢复两条路径进入页面。

> 说明：在做这个功能之前，网页端对 `v5/bag/voice/cmd` 是“安全忽略”的（能正常运行但不展示）。本次是**可选增强**，把它显示出来。

---

## 2. 数据流

```
Jetson 发 v5/bag/voice/cmd
  ├─(实时) 浏览器 useMqttClient 订阅 → store.setLastVoiceCmd → 卡片
  └─(镜像) 服务端 daemon → Redis bag:latest.lastVoiceCmd
              └─ /api/iot/state 返回 lastVoiceCmd → store.fetchInitialState（刷新恢复）→ 卡片
```

与既有 `voice/event`（对话事件）完全相同的套路，只是多了一个 `lastVoiceCmd` 字段；归入语音子系统时间线（更新 `voiceLastSeenAt`，**不**动设备本体 `lastSeenAt`）。

---

## 3. 改了哪些文件 / 哪几行（带 `[语音联动]` 注释）

| 文件 | 行 | 改动 |
|------|----|------|
| `lib/iot/redis-mqtt-daemon.ts` | `:298-307` | 新增 `if (topic === 'v5/bag/voice/cmd')` 分支：`hset bag:latest lastVoiceCmd <json> voiceLastSeenAt <ts>` |
| `app/api/iot/state/route.ts` | `:21`、`:70` | fallback 与正常响应都返回 `lastVoiceCmd`（复用 `parseVoiceEvent` 解析 JSON 字符串） |
| `store/useIoTStore.ts` | `:54-60`(类型)、`:108-109`(接口)、`:138`(初值)、`:194-199`(setter)、`:283-286`(刷新恢复) | 新增 `VoiceCmd` 类型、`lastVoiceCmd` 状态、`setLastVoiceCmd()` |
| `hooks/useMqttClient.ts` | `:18`、`:32`、`:120-124` | topics 增加 `voiceCmd: 'v5/bag/voice/cmd'`（随 `Object.values` 一并订阅）；message 分支 `setLastVoiceCmd(data)` 后 `return`（不 bump 设备 lastSeen） |
| `components/dashboard/voice-assistant-card.tsx` | `:15-32`(describeVoiceCmd)、`:40`、`:101-109` | 读 `lastVoiceCmd`，底部新增「语音指令」一行，命令转中文文案 |

---

## 4. 卡片显示效果

语音助手卡片底部多一行（仅当收到过语音命令时出现）：

```
[语音指令] 进入专注模式
```

`describeVoiceCmd()` 文案映射：

| action / value | 文案 |
|----------------|------|
| `mode_switch` / `focus_mode` | 进入专注模式 |
| `mode_switch` / `normal_mode` | 切换普通模式 |
| `indicator` / `listening` | 唤醒（正在聆听） |
| `indicator` / `thinking` | 思考中 |
| `indicator` / `idle` | 休眠 |
| `screen_text` / `<文字>` | 屏显：`<文字>` |

---

## 5. 不影响命令闭环

- 浏览器**只订阅** `v5/bag/voice/cmd`、**不发布**它（发布方是 Jetson）。这是只读展示，不参与命令下发。
- 浏览器的下发命令仍走 `v5/bag/cmd` + `cmd/ack`（`useIoTStore.publishCommand` 未改），`pendingCmd`/ACK 闭环逻辑**完全不变**。
- `v5/bag/voice/cmd` 上的语音命令**没有 ACK**，不会触发 `cmd/ack` 处理、不会弹命令 toast。

---

## 6. 验证

未在本机跑构建（无 `node_modules`）。请在项目环境校验：

```bash
pnpm install      # 或 npm install
pnpm build        # 或 npx tsc --noEmit 做类型检查
```

运行期验证：
1. 设备端「小乐」被唤醒或说「进入专注模式」；
2. 仪表盘语音助手卡片底部出现「语音指令」行并实时更新；
3. 刷新页面，该行由 `/api/iot/state` 的 `lastVoiceCmd` 恢复（需 Redis + daemon 正常）；
4. `/api/iot/daemon-status` 的 `lastMirroredTopic` 可能出现 `v5/bag/voice/cmd`。

> 若 Redis/daemon 未配置：实时仍可显示（走浏览器 MQTT），但刷新后不恢复——与既有 voice/event 行为一致。

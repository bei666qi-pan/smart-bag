SmartBag V5.0 软硬件对接说明（按当前代码修订版）
0. 文档定位

本文档用于指导 ESP32 / ESP32-S3 / ESP32-CAM 与当前 SmartBag 系统完成联调。
它不是理想设计稿，而是按当前仓库代码和当前页面行为整理出的“可落地版本”。

本文档优先解决 3 件事：

让硬件能稳定连接 MQTT
让网页能实时显示设备状态、传感器和 GPS
让硬件知道当前命令、视频、刷新持久化分别是怎么工作的
1. 系统里有 4 条链路，不要混
1.1 网页访问链路

用户打开的是网页：

主站：https://bag.versecraft.cn
位置页：https://bag.versecraft.cn/location
视觉页：https://bag.versecraft.cn/vision
交互页：https://bag.versecraft.cn/interaction
1.2 硬件 MQTT 链路（ESP32 -> Broker）

硬件只走这条：

Host：mqtt.bag.versecraft.cn
Port：1883
Protocol：TCP MQTT

硬件不要加 http://、https://，也不要连 wss://...

1.3 网页 MQTT 链路（浏览器 -> Broker）

网页内部走的是 WSS。仓库示例环境变量里是：

NEXT_PUBLIC_MQTT_URL=wss://mqtt.bag.versecraft.cn/mqtt
NEXT_PUBLIC_MQTT_PATH=/mqtt

这条链路是给浏览器用的，不是给硬件用的。
线上部署也可能改成别的 WSS 地址，但硬件一律仍然只连 TCP 1883。

1.4 服务端持久化链路（MQTT -> Redis -> API）

当前代码里，服务端还有一条后台链路：

instrumentation.ts 会订阅 v5/bag/#
把收到的状态、传感器、GPS 写入 Redis bag:latest
/api/iot/state 从 Redis 读取初始状态返回给前端

这条链路的作用是：

页面刷新后不至于全部回到默认假值
新打开网页时，能先看到最近一次设备状态
2. 硬件 MQTT 连接参数（必须照着配）
配置项	当前值	说明
Broker Host	mqtt.bag.versecraft.cn	不要带协议头
Broker Port	1883	原生 MQTT TCP 端口
Protocol	MQTT v3.1.1 或 MQTT v5	二选一即可
Client ID	bag_esp32_<MAC后6位>	必须全局唯一
Username	留空	当前默认匿名
Password	留空	当前默认匿名
KeepAlive	60s	推荐值
2.1 必须记住
硬件只连：mqtt.bag.versecraft.cn:1883
不要连 wss://bag.versecraft.cn/mqtt（这是旧入口，可能会导致连接失败）
不要把网页地址当成 MQTT 地址
3. Topic 总表（当前主线）
主题	方向	作用	QoS 建议	Retain 建议
v5/bag/status	设备 -> 云端	在线/离线状态、LWT	1	True
v5/bag/sensors	设备 -> 云端	电量/温度/湿度	0	False
v5/bag/gps	设备 -> 云端	定位坐标	0	False
v5/bag/cmd	云端 -> 设备	下行控制命令	1	False
v5/bag/cmd/ack	设备 -> 云端	命令执行回执	1	False
3.1 为什么只建议 status 开 Retain

status 是网页判断“设备当前在线还是离线”的关键。
Retain 开在 v5/bag/status 上，新订阅者能立即拿到设备最后状态。

sensors 和 gps 当前不建议 Retain。
页面刷新后这两类数据是否还能看到，取决于服务端是否把 MQTT 数据成功镜像到了 Redis。

4. 当前前端实际订阅了哪些 Topic

当前网页 MQTT Hook 会订阅：

v5/bag/status
v5/bag/sensors
v5/bag/gps
v5/bag/cmd/ack

这意味着：

硬件必须持续上报 status / sensors / gps
如果网页下发过命令，硬件还必须回 cmd/ack
网页不会去订阅 v5/bag/cmd，因为这是云端发给硬件的命令 topic，本来就该由设备来订阅
5. 上行协议：设备 -> 云端

所有 payload 一律为 标准 JSON。

5.1 在线 / 离线状态（LWT）

Topic： v5/bag/status

设备连上后立即发送：

{
  "status": "online"
}

连接前请设置遗嘱（Last Will）：

{
  "status": "offline"
}

建议：

QoS = 1
Retain = True

这样设备异常掉线时，网页会自动切到离线状态。

5.2 传感器数据

Topic： v5/bag/sensors

建议频率：

每 3~5 秒 一次即可

推荐 payload：

{
  "battery": 88,
  "temp": 24.5,
  "humid": 45
}

字段要求：

battery：电量，推荐 0~100
temp：温度，数字
humid：湿度，数字

前端当前就是按这 3 个字段名读取的，最好不要改名。

5.3 GPS 数据

Topic： v5/bag/gps

建议频率：

移动中：1 秒/次
静止时：5~10 秒/次

推荐 payload：

{
  "lat": 31.230412,
  "lng": 121.473701
}

当前代码也兼容下面这种写法：

{
  "latitude": 31.230412,
  "longitude": 121.473701
}

因为前端和服务端镜像逻辑都做了 lat/lng 与 latitude/longitude 的兼容读取。

坐标系要求（非常重要）
硬件必须上传 WGS-84 原始坐标
不要在硬件端自己转 GCJ-02

前端位置页会在地图更新时自行调用坐标转换逻辑。

5.4 命令回执 ACK

Topic： v5/bag/cmd/ack

当前前端要求的 ACK 结构是（与页面调试面板一致）：

{"cmd_id":"<same-id>","status":0,"msg":"OK"}

字段说明：

cmd_id：必须回填网页下发命令里的 cmd_id
status：0 表示成功，非 0 表示失败
msg：可选，错误说明或结果说明

当前前端收到 ACK 后，会更新 lastCmdAck、清除 pendingCmd，并给出成功/失败提示。

6. 下行协议：云端 -> 设备

Topic： v5/bag/cmd

当前仓库代码已统一为与软硬协议一致的下行命令格式（网页端 publishCommand 发出的 payload）：

6.1 下发 Topic

v5/bag/cmd

6.2 模式切换命令

{"id":"<unique-id>","action":"mode_switch","value":"focus_mode"}

或：

{"id":"<unique-id>","action":"mode_switch","value":"normal_mode"}

6.3 屏幕文字命令

{"id":"<unique-id>","action":"screen_text","value":"该喝水啦！"}

6.4 硬件解析与回执

- 设备订阅 `v5/bag/cmd`
- 解析 `action` 与 `value`
- 执行后向 `v5/bag/cmd/ack` 回：
  {"cmd_id":"<same-id>","status":0,"msg":"OK"}

7. 页面当前如何显示状态（有助于联调理解）

当前顶部状态栏已经分开显示了：

MQTT：浏览器与 Broker 的 WSS 连接状态
设备：是否收到 v5/bag/status 上报的在线状态
API：/api/iot/state 初始拉取状态

所以联调时要区分：

7.1 MQTT 在线

表示：网页已经连上 Broker
不代表：硬件已经上线

7.2 设备在线

表示：硬件已经向 v5/bag/status 发了 {"status":"online"}
不代表：所有传感器/GPS/视频都正常

7.3 API 正常

表示：网页已能从 /api/iot/state 拿到最近一次 Redis 镜像状态
不代表：当前这一秒仍然在线

8. 刷新后为什么有时还能看到数据，有时又没了

这件事硬件同学也需要知道，因为它直接影响你联调时的判断。

8.1 当前机制

服务端有一个 MQTT -> Redis 镜像逻辑：

收到 v5/bag/status
收到 v5/bag/sensors
收到 v5/bag/gps
就把这些值写入 Redis 的 bag:latest。

然后 /api/iot/state 再从 bag:latest 读出来返回。

8.2 这意味着什么

如果服务端 Redis/MQTT 守护链路没跑起来：

页面刷新后只剩默认值或 fallback
即使实时 MQTT 页面是正常的，刷新后也可能“看起来全没了”
8.3 对硬件联调的建议

如果你发现：

实时 MQTT 有更新
但刷新网页后又回到旧值/空值

这不一定是硬件错，更多时候是：

服务端 Redis 没连上
MQTT_SERVER_URL 没配
instrumentation.ts 没真正运行
9. 视频链路（按当前代码说明）

当前代码支持两种视觉模式。

9.1 LAN 模式：直接拉 ESP32-CAM MJPEG 流

前端会读取：

NEXT_PUBLIC_ESP32_STREAM_URL

如果配的是局域网地址，例如：

http://192.168.1.100:81/stream

那只适合同一局域网里测试。

要求
输出标准 MJPEG 流
浏览器能直接访问
典型路径：http://<ESP32_IP>:81/stream
注意

如果网页是 https://...，而视频流是 http://...，公网页面会遇到 Mixed Content 问题。
也就是说，LAN 模式更适合本地开发，不适合公网正式访问。

9.2 WAN 模式：服务器快照 API

当前代码还提供了快照接口：

POST /api/camera/latest
GET /api/camera/latest
设备上传快照时必须满足
请求头：
x-device-token: bag_secret_2026
Content-Type：
multipart/form-data
文件字段名：
image
成功返回
{
  "success": true,
  "message": "快照上传成功",
  "timestamp": "2026-..."
}
网页拉取方式

网页在 WAN 模式下会周期性请求：

GET /api/camera/latest

如果没有快照，会返回 404 + “暂无快照”。

10. 硬件联调最小清单（必须按顺序过）
10.1 先测 MQTT TCP

用 MQTT 客户端连：

Host：mqtt.bag.versecraft.cn
Port：1883

确认能连上再进行下一步。

10.2 测设备上下线

向 v5/bag/status 发布：

{"status":"online"}

看网页设备状态是否变在线。
再模拟掉线或手动发：

{"status":"offline"}
10.3 测传感器

向 v5/bag/sensors 发：

{"battery":88,"temp":24.5,"humid":45}

看顶部数值是否更新。

10.4 测 GPS

向 v5/bag/gps 发：

{"lat":31.230412,"lng":121.473701}

看位置页地图和坐标是否更新。

10.5 测命令闭环

打开 /interaction 页的 IoT 调试面板，发送：

mode_switch
screen_text

然后硬件订阅 v5/bag/cmd，执行并回：

{
  "cmd_id": "网页收到的那个 cmd_id",
  "status": 0,
  "msg": "OK"
}

看页面是否消除 pending 并显示 ACK 成功。

10.6 测视频

本地局域网先直接打开：

http://<ESP32_IP>:81/stream

若局域网流能跑，再决定是否接公网代理或快照接口。

11. 硬件开发避坑指南（按当前代码补充）
11.1 不要把网页地址当 MQTT 地址

硬件只能连：

mqtt.bag.versecraft.cn:1883
11.2 Client ID 必须唯一

建议：

bag_esp32_<MAC后6位>
11.3 只发 WGS-84 原始坐标

前端会转，硬件不要自己转。

11.4 传感器字段名不要乱改

优先固定：

battery
temp
humid
11.5 GPS 字段优先用 lat/lng

虽然代码兼容 latitude/longitude，但建议统一发：

lat
lng
11.6 当前下行命令按协议真实格式写

下发：id/action/value
11.7 ACK 必须带 cmd_id

否则网页无法把 ACK 对应回待执行命令。

11.8 自动重连必须写

至少要有：

WiFi 断线重连
MQTT 断线重连
11.9 视频快照接口目前 token 是固定值

当前代码里写死的是：

x-device-token: bag_secret_2026

这对联调是必须信息，但从安全角度看，后续建议改成环境变量。

12. 给硬件同学的一页总结
你最先要记住的 8 条
硬件 MQTT 只连：mqtt.bag.versecraft.cn:1883
网页 WSS 跟你无关，不要连 wss://...
上行 topic 就这 4 个最关键：
v5/bag/status
v5/bag/sensors
v5/bag/gps
v5/bag/cmd/ack
下行命令从 v5/bag/cmd 收
当前代码里的命令格式是：
id + action + value
ACK 必须带：
cmd_id
status
msg
GPS 只发 WGS-84，别自己转坐标
视频先搞定局域网 MJPEG，再谈公网